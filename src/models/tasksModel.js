// const connection = require('./connection');

const getAll = async () => {
  // const buscando = await connection.execute('SELECT * FROM tasks');
  // return buscando;
  return "OK"
};

module.exports = {
  getAll
};